# VIM Syntax Highlighting for TWML

This repository contains a VIM syntax file for [TWML](https://github.com/DISTREAT/twml).

# Installation

Copy `syntax/twml.vim` and `fdetect/twml.vim` to `$HOME/.vim` or `$XDG_DATA_CONFIG/nvim`.
